name = "tier2tickets-relay"
main = "src/index.ts"
compatibility_date = "2024-11-27"
compatibility_flags = ["nodejs_compat"]

# Workers Logs: capture console.log/console.warn in the dashboard for debugging.
# Source-level logging is silent by default — only non-PII breadcrumbs are emitted;
# full request/response bodies require DEBUG_LOGS=true (see src/log.ts).
[observability.logs]
enabled = true
# invocation_logs capture per-request metadata (method, path, status, timing, CF
# headers) at the PLATFORM level, REGARDLESS of the source-level silence above.
# Left ENABLED deliberately (audit F4): disabling it would blind ops to request
# volume/latency/errors. This is a conscious operator decision for a PHI-adjacent
# service — set invocation_logs=false if request-metadata retention is unacceptable,
# and/or lower head_sampling_rate below to retain only a fraction of invocations.
invocation_logs = true
# Fraction of invocations whose logs are retained (0.0–1.0). 1.0 = keep all.
# Lower it to reduce retained request metadata (another conscious ops decision).
head_sampling_rate = 1.0

# Two crons (differentiated in src/index.ts `scheduled` by `event.cron`):
#   "0 */6 * * *"  refreshes the D1 mirror of Gorelo agents/clients (idempotent syncAll).
#   "*/5 * * * *"  flushes orphaned deferred tickets — a press whose /actions note never
#                  arrived — creating them note-less after the PENDING_GRACE_MS window.
[triggers]
crons = ["0 */6 * * *", "*/5 * * * *"]

# Location fetches have no bulk Gorelo endpoint, so they're one call per client.
# Doing all of them inline blows the Worker's 50 external-subrequest/invocation
# cap (free plan) at scale, so syncAll enqueues one message per client here and a
# queue consumer fetches locations in small batches — each consumer invocation
# gets its own subrequest budget. Queues are on the free plan (10k ops/day).
[[queues.producers]]
binding = "SYNC_QUEUE"
queue = "tier2tickets-sync"

[[queues.consumers]]
queue = "tier2tickets-sync"
# <=10 clients per batch -> <=10 location fetches per invocation, well under 50.
max_batch_size = 10
max_batch_timeout = 10
# Per-message retry with backoff replaces the hand-rolled fetch retry loop for
# locations; give a rate-limited client a few tries before giving up this cycle.
max_retries = 3

[vars]
# US: https://api.usw.gorelo.io   AU: https://api.aue.gorelo.io
GORELO_BASE_URL = "https://api.usw.gorelo.io"

# When "true" (default), only the source IPs of the enabled products below (matched
# via CF-Connecting-IP) may POST tickets. "false"/"0"/"" disables the allowlist.
ENFORCE_IP_ALLOWLIST = "true"

ALERT_SOURCES    = "default, nysphi"
ALERT_IPS_NYSPHI = "69.74.23.178, 47.21.134.136"
# Per-product allowlist toggles. "true" enables that product's source IPs/CIDR
# ranges (matched via CF-Connecting-IP); any other value disables. Product keys
# and IPs live in src/products.ts (PRODUCTS). An unset flag falls back to the
# product's built-in default: tier2 on (backward-compatible), huntress off (opt-in).
# NOTE: enabling a product is only the IP doorman — the ticket-building path is
# still Tier2/Helpdesk-Buttons-shaped, so a newly enabled product needs its own
# field handling before its requests produce correct Gorelo tickets.
ENABLE_TIER2 = "true"
ENABLE_HUNTRESS = "true"

# --- Gorelo IDs: fill these from scripts/gorelo-ids.sh (values below are from the live Salient tenant) ---
# groupId / typeId / priorityId / sourceId are NON-NULLABLE on CreatePublicTicketCommand
# and must always be sent, so these have no safe zero-default.
#
# Groups (from the Gorelo UI): 2051 "Everyone" (general help queue, help@salientmsp.com),
# 2052 "Escalations", 4084 "Security". Button presses go to the general queue.
# (GET /v1/organization/groups returns 403 for this key — the Worker never calls it at
# runtime, so no scope change is needed; the id below was taken from the UI.)
DEFAULT_GROUP_ID = "2051"

# GET /v1/tickets/types -> 7045 "Incident" (unplanned interruption; fits a button press).
# Alternatives: 7046 Request, 7047 Proactive, 7048 Internal, 8412 Outage. (7045 confirmed accepted.)
DEFAULT_TYPE_ID = "7045"

# GET /v1/tickets/statuses -> 1 "New". REQUIRED by Gorelo's validator: a create WITHOUT
# statusId returns HTTP 400 (confirmed by testing), even though the swagger marks it nullable.
DEFAULT_STATUS_ID = "1"

# Status a Huntress-resolution notice lands in. Huntress signals a resolution by EDITING
# the source ticket to its configured "Status after Huntress Resolution"; Gorelo has no
# ticket-update API, so we file a labeled resolution ticket in this status instead (and
# mark the original resolved in our ledger). Set to your Gorelo "Resolved"/"Closed" status
# id (GET /v1/tickets/statuses). Unset -> falls back to DEFAULT_STATUS_ID (notice still
# files, just not in a closed status).
# DEFAULT_RESOLVED_STATUS_ID = "5"

# PublicTicketPriority enum = [0,1,2,3,4]. Ships WITHOUT labels in the spec.
# TODO(verify): confirm which int is your desired default priority in the Gorelo UI. 2 = a middle default.
DEFAULT_PRIORITY = "2"

# TicketSource enum = [1,2,3,4,5,6]. 6 = the API/integration source (Gorelo tags these with a
# cog icon); confirmed accepted by the create endpoint.
DEFAULT_SOURCE = "6"

# Fallback client when no machine/domain match is found.
# GET /v1/clients -> 15567 "Salient MSP" (isDefault:true) — unmatched presses land on the
# MSP's own default client with a triage note for a tech to reassign.
CATCHALL_CLIENT_ID = "15567"

# Per-product "Submitted via …" tags (tagIds on the create command) — lets you
# filter/report on tickets by the source that submitted them. Each ticket is tagged with
# its own product's tag; a product with no tag of its own falls back to FALLBACK_TAG_ID
# below. These are TAGS (not the ticket type; DEFAULT_TYPE_ID stays 7045 "Incident").
# Unset any of them to skip that tag.
# 31974 "Submitted VIA HDB" — Helpdesk Buttons / Tier2 tickets.
HDB_TAG_ID = "31974"
# 32870 "Submitted via Huntress" — Huntress tickets.
HUNTRESS_TAG_ID = "32870"
# 32885 "Submitted via API" — catch-all applied when a ticket's product has no tag of its
# own (a service onboarded before its dedicated tag is wired up, or a request that matched
# no product). Ensures nothing is left untagged if we forget to add a per-service tag.
FALLBACK_TAG_ID = "32885"

# Priority id used when a press is flagged "This is an emergency" (else DEFAULT_PRIORITY).
# PublicTicketPriority enum [0..4]; 1 is the top priority in the Gorelo UI ("Critical").
# Unset to always use DEFAULT_PRIORITY.
EMERGENCY_PRIORITY = "1"

# Ask Gorelo to send its "ticket created" email to the requester. "false" (default)
# never sends it. "true" sends it ONLY when the Worker resolved a real client contact
# — a press that falls back to the catch-all client (no contact match) never triggers
# the email, so it can't notify the wrong party.
SEND_TICKET_CREATED_EMAIL = "true"

# Verbose logging toggle. "false" (default) logs only non-PII breadcrumbs (method/path/
# status + resolved ids). Set "true" to log full HALO CAPTURE/RESPONSE bodies — which
# contain PII/PHI (names, emails, phones) — only for short debugging windows.
DEBUG_LOGS = "false"

# Egress Jira fan-out for co-managed clients (src/egress/jira/). Master switch:
# "true" mirrors each created ticket into the enrolled client's own Jira Cloud (in
# addition to the Gorelo ticket) and closes that Jira issue when the ticket resolves.
# "false" (default) disables it entirely. Which clients are enrolled is set per-client
# in the JIRA_TARGETS secret — a client with no entry there is never sent to Jira even
# when this is "true". The Gorelo ticket path is unchanged either way: a Jira failure
# is queued for the */5 cron flush, never blocking a create.
ENABLE_JIRA = "false"

# Master on/off for the Sentry error monitor (src/index.ts). The integration is OFF
# unless this is truthy ("true"/"1"/"yes"/"on") — off, Sentry sends nothing (no events,
# no spans, no egress). Set "true" here to enable it for the deployed Worker; tests and
# `wrangler dev` leave it unset, so they never emit to Sentry. Sentry is configured to
# ship NO PII/PHI (no bodies, headers, cookies, or user/IP) — see sentryOptions().
SENTRY_ENABLED = "true"

# --- Monitoring alerts endpoint (POST /v1/alerts) ---------------------------------
# A generic HTTPS ingress for monitoring sources (e.g. an on-prem Windows/SQL Server)
# to raise/update/resolve Gorelo native alerts (POST /v1/alerts/ — an alert, not a
# ticket; the relay owns dedup + lifecycle). Each "source" (a customer) is TWO-FACTOR:
# it has its own shared secret bound to its own IP allowlist, so a request must present
# that source's secret AND come from that source's IP(s) — a wrong secret is 401, a
# valid secret from the wrong IP is 403. See README "Monitoring alerts".
#
# ALERT_SOURCES lists the source keys (unset => just the built-in `default`). Per key:
#   default -> ALERT_SHARED_SECRET (secret) + ALERT_ALLOWED_IPS (var, below)
#   <key>   -> ALERT_SECRET_<KEY>  (secret) + ALERT_IPS_<KEY>    (var)
#             (KEY = the key upper-cased, non-alphanumerics -> "_")
# Per-source vars are read by name, so add a customer WITHOUT a code change: append its
# key here, set ALERT_IPS_<KEY> below, and `wrangler secret put ALERT_SECRET_<KEY>`.
# ALERT_SOURCES = "default, acme"
#
# `default` source IPs: exact IPs and/or IPv4 CIDR ranges (comma/space/newline separated,
# matched via CF-Connecting-IP). Enforced only while ENFORCE_IP_ALLOWLIST is on; empty
# while enforced => the default source is rejected. Add the source's public egress IP,
# e.g. the NAT/egress address the monitoring host dials out from.
ALERT_ALLOWED_IPS = ""
# Per-customer source IP allowlists (paired with the ALERT_SECRET_<KEY> secret). Each is
# a LIST — any mix of exact IPs and IPv4 CIDR ranges/subnets, comma/space/newline separated:
# ALERT_IPS_ACME = "203.0.113.7, 198.51.100.0/24, 192.0.2.0/28"
# Optional Gorelo client id alerts are raised against (PostAlertRequest.ClientId).
# Unset => the alert's `customer` is matched by exact name against the client mirror,
# then the `host` against a mirrored device, else CATCHALL_CLIENT_ID.
# ALERT_CLIENT_ID = "15567"
# Severity->Gorelo AlertLevel is a FIXED, hardcoded mapping (Gorelo's level enum is not
# tenant-customizable): critical->1 (Critical), warning->3 (Warning), info->4 (Info/Low).
# See alertLevel() in src/alerts.ts. There is no env override.

# Bearer-token enforcement on the Halo resource endpoints (audit F1). Only takes
# effect when BOTH HALO_CLIENT_ID and HALO_CLIENT_SECRET secrets are set; the
# gate is skipped entirely otherwise, and /token is always exempt.
#   "off"     (DEFAULT) no token check — identical to legacy behavior.
#   "observe"          verify the Authorization: Bearer token and log a non-PII
#                      breadcrumb (present/missing/invalid/expired) — never rejects.
#   "enforce"          reject with 401 { "error": "invalid_token" } when the token
#                      is missing/invalid/expired on any non-/token resource.
# Rollout: deploy "off" -> switch to "observe" and confirm from breadcrumbs that
# real Tier2 traffic round-trips a valid token -> only then set "enforce".
# Per-product (issue #51): the gate is applied against the MATCHED product's own
# credential pair, so with per-product creds set, "enforce" works with multiple
# products at once — each authenticates with its own client_id/secret.
HALO_TOKEN_ENFORCE = "enforce"

# Non-secret per-product OAuth client_ids (the client_secret goes in via `wrangler
# secret put` — see below). The client_id is a public identifier, so it can live
# here. Uncomment/fill per enabled product; both the id AND the secret must resolve
# for that product to be validated + token-enforced (else it stays lenient).
#   HALO_CLIENT_ID          — tier2's client_id (its var is un-suffixed; secret is HALO_CLIENT_SECRET)
#   HALO_CLIENT_ID_HUNTRESS — Huntress's client_id (e.g. "13b3832f9306b677fb72eea412da5df7")
# Generate a pair + push the secret with:  ./scripts/halo-cred.sh <product>
#   (Windows PowerShell: ./scripts/halo-cred.ps1 <product>)
# HALO_CLIENT_ID_HUNTRESS = "13b3832f9306b677fb72eea412da5df7"

# --- Secrets (set via CLI, never committed) ---
#   wrangler secret put GORELO_API_KEY      # X-API-Key for Gorelo (ticket write + asset/contact/client read scopes)
#   wrangler secret put ADMIN_KEY           # gates POST /admin/sync (X-Admin-Key / X-API-Key / Authorization: Bearer)
#   wrangler secret put NOTIFLY_URLS        # optional: notifly (Apprise) URL(s) alerted when a ticket is dead-lettered
#                                           #   e.g. "ntfy://alerts" | Teams Workflows "workflows://…&sig=…" — comma/space separated
#   wrangler secret put JIRA_TARGETS        # optional: per-client Jira destinations for the egress Jira fan-out (with ENABLE_JIRA="true")
#   wrangler secret put ALERT_SHARED_SECRET # `default` source secret for POST /v1/alerts (Bearer / X-Alert-Key)
#   wrangler secret put ALERT_SECRET_<KEY>  # per-customer source secret (one per ALERT_SOURCES key, paired with ALERT_IPS_<KEY>)
# Per-product Halo mock OAuth secrets (issue #51) — one client_secret per product,
# paired with the client_id var above. Easiest: ./scripts/halo-cred.sh <product>
#   (Windows PowerShell: ./scripts/halo-cred.ps1 <product>)
#   wrangler secret put HALO_CLIENT_SECRET          # tier2's client_secret (validated with HALO_CLIENT_ID)
#   wrangler secret put HALO_CLIENT_SECRET_HUNTRESS # Huntress's client_secret (validated with HALO_CLIENT_ID_HUNTRESS)

# --- D1 ---
# Create once:  wrangler d1 create tier2tickets-relay
# then paste the returned database_id below.
[[d1_databases]]
binding = "DB"
database_name = "tier2tickets-relay"
database_id = "fe2efa6c-f7d4-439b-adfd-3dc10ee0caa5"
migrations_dir = "migrations"

# =====================================================================================
# STAGING ENVIRONMENT — a separate Cloudflare tenant.
# Deploy with:  npm run deploy:staging   (wrangler deploy --env staging)
#
# Everything above this line is PRODUCTION and is what a bare `wrangler deploy` uses.
# Wrangler does NOT inherit `vars`, `d1_databases` or `queues` into an environment — it
# warns and the binding is simply ABSENT — so every one of them is repeated below.
# (`account_id` and `triggers` ARE inherited, so both are restated explicitly here to
# keep staging from silently following production.)
# `npm run check:configs` fails if this env stops covering a production var, or if any
# of its identifiers point back at the production tenant.
#
# FIRST-TIME SETUP (once, against the STAGING tenant):
#   1. Fill account_id below with the staging Cloudflare account id.
#   2. wrangler d1 create tier2tickets-relay-staging --env staging
#        -> paste the returned id into database_id below
#   3. wrangler queues create tier2tickets-sync-staging --env staging
#   4. wrangler d1 migrations apply tier2tickets-relay-staging --env staging --remote
#   5. Fill the TODO(staging) ids below from the STAGING Gorelo tenant:
#        GORELO_API_KEY=<staging key> ./scripts/gorelo-ids.sh
#   6. Push the staging secrets (each needs --env staging; they are per-environment
#      and do NOT carry over from production):
#        wrangler secret put GORELO_API_KEY --env staging   # the STAGING tenant's key
#        wrangler secret put ADMIN_KEY --env staging
#        wrangler secret put ALERT_SHARED_SECRET --env staging
#        wrangler secret put HALO_CLIENT_SECRET --env staging
#      (./scripts/halo-cred.sh <product> --env staging already passes --env through.)
# =====================================================================================
[env.staging]
name = "tier2tickets-relay-staging"
# TODO(staging): the STAGING Cloudflare account id. Deploys fail loudly until it is set,
# which is deliberate — without it, `--env staging` would publish into whichever tenant
# you are currently logged into, i.e. production.
account_id = "c6452865d04ed5bd485084005c60cb02"

[env.staging.observability.logs]
enabled = true
invocation_logs = true
head_sampling_rate = 1.0

# Inherited from production if omitted, so stated explicitly.
[env.staging.triggers]
crons = ["0 */6 * * *", "*/5 * * * *"]

# Distinct queue name. Different tenants have separate queue namespaces, so this is
# defense in depth: if account_id were ever wrong, a shared name would let staging
# consume production's messages.
[[env.staging.queues.producers]]
binding = "SYNC_QUEUE"
queue = "tier2tickets-sync-staging"

[[env.staging.queues.consumers]]
queue = "tier2tickets-sync-staging"
max_batch_size = 10
max_batch_timeout = 10
max_retries = 3

[env.staging.vars]
# Gorelo's base URL is REGIONAL, not per-tenant — the tenant is selected by the
# GORELO_API_KEY secret. So staging keeps the same regional URL and points at the
# staging tenant purely via its own key (step 6 above). Change this only if the staging
# tenant lives in another region (AU: https://api.aue.gorelo.io).
GORELO_BASE_URL = "https://api.usw.gorelo.io"

# ON, mirroring production. Staging is a public, internet-reachable Worker whose
# endpoints file tickets, so it keeps production's doorman. Add a test source's egress
# IP to the product ranges (src/products.ts) or to ALERT_ALLOWED_IPS below to exercise it.
ENFORCE_IP_ALLOWLIST = "true"

ENABLE_TIER2 = "true"
ENABLE_HUNTRESS = "true"

# OFF for staging. Staging tenants are usually seeded from production, so a real client's
# JIRA_TARGETS entry would mirror test tickets into their live Jira. Enable deliberately
# (with a staging-only JIRA_TARGETS) only when exercising the fan-out end to end.
ENABLE_JIRA = "false"

# --- Gorelo IDs: TENANT-SPECIFIC, must be re-derived against the STAGING tenant -------
# Run `GORELO_API_KEY=<staging key> ./scripts/gorelo-ids.sh` and paste the results.
# "0" is a deliberate placeholder: staging is not functional until these are filled.
DEFAULT_GROUP_ID = "0"       # TODO(staging): group id from the staging tenant
DEFAULT_TYPE_ID = "0"        # TODO(staging): ticket type id ("Incident" in prod)
CATCHALL_CLIENT_ID = "0"     # TODO(staging): fallback client id
HDB_TAG_ID = "0"             # TODO(staging): "Submitted VIA HDB" tag id
HUNTRESS_TAG_ID = "0"        # TODO(staging): "Submitted via Huntress" tag id
FALLBACK_TAG_ID = "0"        # TODO(staging): "Submitted via API" tag id

# Fixed Gorelo enums / universal ids — NOT tenant-specific, so these carry over as-is.
# PublicTicketPriority = [0..4]; TicketSource = [1..6] (6 = API/integration); status 1 = New.
DEFAULT_STATUS_ID = "1"
DEFAULT_PRIORITY = "2"
DEFAULT_SOURCE = "6"
EMERGENCY_PRIORITY = "1"

# OFF for staging, and the single most important guard here. A staging Gorelo tenant is
# usually seeded from production, so its contacts carry REAL email addresses — leaving
# this on would have staging email real people about test tickets.
SEND_TICKET_CREATED_EMAIL = "false"

# OFF for staging. These logs persist in Cloudflare Workers Logs, and full capture
# bodies are PII/PHI. Flip to "true" only for a short, deliberate debugging window.
DEBUG_LOGS = "false"

# OFF for staging. The Sentry DSN is hardcoded, so enabling it here would mix staging
# noise into the same project that carries real production errors. Enabling staging
# reporting properly means giving it its own DSN first.
SENTRY_ENABLED = "false"

# ON, mirroring production — validating the real token posture is the point of staging.
# Its credential pair is a STAGING secret: ./scripts/halo-cred.sh tier2 --env staging
HALO_TOKEN_ENFORCE = "enforce"

# Alert-source IPs for the staging monitoring ingress. Enforced (allowlist is on above),
# so an empty list rejects the `default` source until a test host's egress IP is added.
ALERT_ALLOWED_IPS = ""

[[env.staging.d1_databases]]
binding = "DB"
database_name = "tier2tickets-relay-staging"
# TODO(staging): paste the id from `wrangler d1 create tier2tickets-relay-staging --env staging`.
# The nil UUID until then, so a deploy fails rather than binding anything real.
database_id = "a7c245d5-25c2-412c-9547-3bba704852ab"
migrations_dir = "migrations"
